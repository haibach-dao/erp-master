export default function AuditPage() {
  return (
    <section>
      <h1 className="text-xl font-semibold">Audit &amp; Check Log</h1>
      <p className="mt-2 text-sm text-muted-foreground">Khung rỗng — nối audit API ở T10.</p>
    </section>
  );
}
